// Copy to wh/Secrets.xcconfig and fill in values (Secrets.xcconfig is gitignored).

SUPABASE_URL = https:/$()/taxvfpiaplablxurktza.supabase.co
SUPABASE_ANON_KEY = your-supabase-anon-key

// Grok is used by the discover-enrich edge function (set in Supabase secrets).
// Keep out of the iOS app once edge function is deployed.
GROK_API_KEY = your-grok-api-key
